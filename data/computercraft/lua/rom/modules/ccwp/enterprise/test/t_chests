local t_chests = {}

local corelog = require "corelog"

local Callback = require "obj_callback"

local Location = require "obj_location"

local T_Chest = require "test.t_mobj_chest"

local enterprise_turtle = require "enterprise_turtle"
local enterprise_projects = require "enterprise_projects"
local enterprise_chests = require "enterprise_chests"

function t_chests.T_All()
    t_chests.T_DeleteChests()
    t_chests.T_SaveGetAndRemoveChest()
end

local testStartLocation  = Location:new({_x= -6, _y= 0, _z= 1, _dx=0, _dy=1})
local testStartLocation2  = Location:new({_x= -6, _y= 6, _z= 1, _dx=0, _dy=1})
local multipleStacksItemsQuery = {
    ["minecraft:birch_log"] = 80,
    ["minecraft:torch"]     = 1,
}

local callback = Callback:new({
    _moduleName     = "t_main",
    _methodName     = "Func1_Callback",
    _data           = { },
})

function t_chests.T_DeleteChests()
    -- prepare test
    corelog.WriteToLog("* enterprise_chests.DeleteChests test")

    -- test
    enterprise_chests.DeleteChests()
    local nChests = enterprise_chests.GetNumberOfChests_Att()
    local expectedNChests = 0
    assert(nChests == expectedNChests, "gotten nChests(="..nChests..") not the same as expected(="..expectedNChests..")")

    -- cleanup test
end

function t_chests.T_SaveGetAndRemoveChest()
    -- prepare test
    local chest = T_Chest.CreateChest() if not chest then corelog.Error("failed obtaining Chest") return end
    local originalNChests = enterprise_chests.GetNumberOfChests_Att()

    -- SaveChest test
    corelog.WriteToLog("* enterprise_chests.SaveChest test")
    local chestLocator = enterprise_chests.SaveChest(chest)
    local nChests = enterprise_chests.GetNumberOfChests_Att()
    local expectedNChests = originalNChests + 1
    assert(nChests == expectedNChests, "gotten nChests(="..nChests..") not the same as expected(="..expectedNChests..")")

    -- GetChest test
    corelog.WriteToLog("* enterprise_chests.GetChest test")
    local chestGotton = enterprise_chests.GetChest(chestLocator)
    local isSame = chestGotton:isSame(chest)
    local expectedIsSame = true
    assert(isSame == expectedIsSame, "gotten isSame(="..tostring(isSame)..") not the same as expected(="..tostring(expectedIsSame)..")")

    -- RemoveChest test
    corelog.WriteToLog("* enterprise_chests.RemoveChest test")
    enterprise_chests.RemoveChest(chestLocator)
    nChests = enterprise_chests.GetNumberOfChests_Att()
    expectedNChests = originalNChests
    assert(nChests == expectedNChests, "gotten nChests(="..nChests..") not the same as expected(="..expectedNChests..")")

    -- cleanup test
end

function t_chests.T_RegisterAndUpdateChest()
    corelog.WriteToLog("* Test Register and Update chest")

    -- create project
    local projectData = {
        chestLocation        = testStartLocation2:getRelativeLocation(2, 5, 0),
        chestAccessDirection = "back"
    }
    local projectServiceData = {
        projectDef  = t_chests.GetRegisterAndUpdateChestProjectDef(),
        projectData = projectData,
    }

    -- start project
    return enterprise_projects.StartProject_ASrv(projectServiceData, callback)
end

function t_chests.GetRegisterAndUpdateChestProjectDef()
    return {
        steps = {
            { stepType = "SSrv", stepTypeDef = { moduleName = "enterprise_chests", serviceName = "RegisterChest_SSrv" }, stepDataDef = {
                { keyDef = "location"           , sourceStep = 0, sourceKeyDef = "chestLocation" },
                { keyDef = "accessDirection"    , sourceStep = 0, sourceKeyDef = "chestAccessDirection" },
            }},
            { stepType = "ASrv", stepTypeDef = { moduleName = "enterprise_chests", serviceName = "UpdateChestRecord_ASrv" }, stepDataDef = {
                { keyDef = "chestLocator"       , sourceStep = 1, sourceKeyDef = "chestLocator" },
            }},
            { stepType = "SSrv", stepTypeDef = { moduleName = "enterprise_projects", serviceName = "AreAllTrue_QSrv" }, stepDataDef = {
                { keyDef = "success1"           , sourceStep = 1, sourceKeyDef = "success" },
                { keyDef = "success2"           , sourceStep = 2, sourceKeyDef = "success" },
            }},
        },
        returnData = {
            { keyDef = "success"                , sourceStep = 3, sourceKeyDef = "success" },
            { keyDef = "chestLocator"           , sourceStep = 1, sourceKeyDef = "chestLocator" },
        }
    }
end

local chestLocator1 = nil

function t_chests.T_UpdateChestRecord1()
    corelog.WriteToLog("* Test UpdateChestRecord_ASrv 1")
    -- create chest data
    local serviceData = {
        chestLocator = chestLocator1,
    }

    -- call test method
    corelog.WriteToLog("  calling enterprise_chests.UpdateChestRecord_ASrv("..textutils.serialize(serviceData)..", , )")
    return enterprise_chests.UpdateChestRecord_ASrv(serviceData, callback)
end

function t_chests.T_RegisterChest1()
    corelog.WriteToLog("* Test RegisterChest_SSrv 1:")
    -- create chest data
    local chestData  = {
        location        = testStartLocation:getRelativeLocation(2, 5, 0),
        accessDirection = "top"
    }

    -- call test method
    corelog.WriteToLog("  calling enterprise_chests.RegisterChest_SSrv("..textutils.serialize(chestData)..")")
    local result = enterprise_chests.RegisterChest_SSrv(chestData, callback)
    corelog.WriteToLog("  result="..textutils.serialize(result))
    if result.success then
        chestLocator1 = result.chestLocator
    end
end

function t_chests.T_DelistChest1()
    corelog.WriteToLog("* Test DelistChests 1:")
    -- create chest data 1
    local chestData1 = {
        chestLocator = chestLocator1
    }

    -- call test method
    corelog.WriteToLog("  calling enterprise_chests.DelistChest_ASrv("..textutils.serialize(chestData1)..")")
    return enterprise_chests.DelistChest_ASrv(chestData1, callback)
end

return t_chests
